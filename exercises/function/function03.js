import assert from "assert"

/*
Nested functions
*/

// I AM NOT DONE

function hypotenuse(a, b) {
  // TODO: Define a nested function to complete the code
  ...

  return Math.sqrt(square(a) + square(b))
}

assert.equal(hypotenuse(3, 4), 5)
